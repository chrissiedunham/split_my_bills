
<a class="bill-link" href="#"><%= bill.escape('name') %></a>
<span class="delete-bill-link" >X</span>
<div class="bill-show hidden">
  <div>Amount: <%= bill.escape('amount') %></div>

  <% debtors.forEach(function(debtor){ %>
    <%= debtor.escape('name') %> owes <%= debtor.escape('amount_owed') %>
  <% }) %>

  <div>
    <button class="edit-bill btn btn-default" type="submit">Edit bill</button>
  </div>

</div>
